export const EMAIL_CONFIG = {
    user: process.env.EMAIL || "fbl@kensciomail.com", // Gmail address
    appPassword: process.env.APP_PASSWORD || "your-app-password", // App password
    imapHost: "imap.gmail.com", // IMAP host for Gmail
    imapPort: 993, // IMAP port for Gmail
  };
  